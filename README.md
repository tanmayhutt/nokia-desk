# Nokia Desk

A browser-based Nokia 3310 emulator built with React and Vite.

The phone includes classic keypad navigation, a boot sequence, portfolio contacts and projects, a small terminal, cover colors, procedural audio, and playable Snake.

```bash
npm install
npm run dev
```

The production build uses the `/nokia-desk/` base path for GitHub Pages.
